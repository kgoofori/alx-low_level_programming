#include "main.h"

/**
 * puts_half - find the length of a string
 * @s: string pointer
 * Return: int
*/

void puts_half(char *str)
{

	int i = 0;

	while (s[i])
	{
		i++;
	}

	i = i / 2;

	while (str[i])
	{
		_putchar(str[i]);
		i++;
	}

	_putchar('\n');

}
